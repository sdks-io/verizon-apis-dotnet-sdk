// <copyright file="APIController.cs" company="APIMatic">
// Verizon.Standard
//
// This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
// </copyright>
using APIMatic.Core;
using APIMatic.Core.Types;
using APIMatic.Core.Utilities;
using System;
using System.Collections.Generic;
using System.Net.Http;
using System.Threading;
using System.Threading.Tasks;
using Verizon.Standard.Exceptions;
using Verizon.Standard.Http.Response;

namespace Verizon.Standard.Controllers
{
    /// <summary>
    /// APIController.
    /// </summary>
    public class APIController : BaseController
    {
        /// <summary>
        /// Initializes a new instance of the <see cref="APIController"/> class.
        /// </summary>
        internal APIController(GlobalConfiguration globalConfiguration) : base(globalConfiguration) { }

        /// <summary>
        /// This endpoint fetches and returns the list of configurations defined by the Vendor. The list contains the configurations' identifier, name, description, and active flag. The vendor ID is provided when the configuration is created through the POST request.
        /// Note: The user needs to authenticate with their ThingSpace credentials using the Access/Bearer and Session/M2M tokens in order to call this API.
        /// </summary>
        /// <param name="vendorID">Required parameter: The vendor's identifier.</param>
        /// <returns>Returns the ApiResponse of List of Models.ConfigurationListItem response from the API call.</returns>
        public ApiResponse<List<Models.ConfigurationListItem>> GetConfigurationList(
                string vendorID)
            => CoreHelper.RunTask(GetConfigurationListAsync(vendorID));

        /// <summary>
        /// This endpoint fetches and returns the list of configurations defined by the Vendor. The list contains the configurations' identifier, name, description, and active flag. The vendor ID is provided when the configuration is created through the POST request.
        /// Note: The user needs to authenticate with their ThingSpace credentials using the Access/Bearer and Session/M2M tokens in order to call this API.
        /// </summary>
        /// <param name="vendorID">Required parameter: The vendor's identifier.</param>
        /// <param name="cancellationToken"> cancellationToken. </param>
        /// <returns>Returns the ApiResponse of List of Models.ConfigurationListItem response from the API call.</returns>
        public async Task<ApiResponse<List<Models.ConfigurationListItem>>> GetConfigurationListAsync(
                string vendorID,
                CancellationToken cancellationToken = default)
            => await CreateApiCall<List<Models.ConfigurationListItem>>()
              .Server(Server.IMPServer)
              .RequestBuilder(requestBuilder => requestBuilder
                  .Setup(HttpMethod.Get, "/api/v1/application/configurations/geofence/ids")
                  .WithAndAuth(andAuth => andAuth
                      .Add("thingspace_oauth")
                      .Add("sessionToken")
                  )
                  .Parameters(parameters => parameters
                      .Header(header => header.Setup("VendorID", vendorID))))
              .ResponseHandler(responseHandler => responseHandler
                  .ErrorCase("403", CreateErrorCase("Forbidden", (errorReason, context) => new EtxResponseErrorException(errorReason, context)))
                  .ErrorCase("404", CreateErrorCase("Configuration not found", (errorReason, context) => new EtxResponseErrorException(errorReason, context)))
                  .ErrorCase("429", CreateErrorCase("Too many requests", (errorReason, context) => new EtxResponseErrorException(errorReason, context)))
                  .ErrorCase("0", CreateErrorCase("unexpected error", (errorReason, context) => new EtxResponseErrorException(errorReason, context))))
              .ExecuteAsync(cancellationToken).ConfigureAwait(false);

        /// <summary>
        /// This endpoint fetches and returns a specific configuration's details. The configuration ID parameter, which was provided when the configuration was created through the POST request, is need to retrieve the configuration details.
        /// Note: The user needs to authenticate with their ThingSpace credentials using the Access/Bearer and Session/M2M tokens in order to call this API.
        /// </summary>
        /// <param name="id">Required parameter: The configuration identifier.</param>
        /// <param name="vendorID">Required parameter: The vendor's identifier.</param>
        /// <returns>Returns the ApiResponse of Models.GeoFenceConfigurationResponse response from the API call.</returns>
        public ApiResponse<Models.GeoFenceConfigurationResponse> GetConfiguration(
                string id,
                string vendorID)
            => CoreHelper.RunTask(GetConfigurationAsync(id, vendorID));

        /// <summary>
        /// This endpoint fetches and returns a specific configuration's details. The configuration ID parameter, which was provided when the configuration was created through the POST request, is need to retrieve the configuration details.
        /// Note: The user needs to authenticate with their ThingSpace credentials using the Access/Bearer and Session/M2M tokens in order to call this API.
        /// </summary>
        /// <param name="id">Required parameter: The configuration identifier.</param>
        /// <param name="vendorID">Required parameter: The vendor's identifier.</param>
        /// <param name="cancellationToken"> cancellationToken. </param>
        /// <returns>Returns the ApiResponse of Models.GeoFenceConfigurationResponse response from the API call.</returns>
        public async Task<ApiResponse<Models.GeoFenceConfigurationResponse>> GetConfigurationAsync(
                string id,
                string vendorID,
                CancellationToken cancellationToken = default)
            => await CreateApiCall<Models.GeoFenceConfigurationResponse>()
              .Server(Server.IMPServer)
              .RequestBuilder(requestBuilder => requestBuilder
                  .Setup(HttpMethod.Get, "/api/v1/application/configurations/geofence")
                  .WithAndAuth(andAuth => andAuth
                      .Add("thingspace_oauth")
                      .Add("sessionToken")
                  )
                  .Parameters(parameters => parameters
                      .Query(query => query.Setup("id", id))
                      .Header(header => header.Setup("VendorID", vendorID))))
              .ResponseHandler(responseHandler => responseHandler
                  .ErrorCase("403", CreateErrorCase("Forbidden", (errorReason, context) => new EtxResponseErrorException(errorReason, context)))
                  .ErrorCase("404", CreateErrorCase("Configuration not found", (errorReason, context) => new EtxResponseErrorException(errorReason, context)))
                  .ErrorCase("429", CreateErrorCase("Too many requests", (errorReason, context) => new EtxResponseErrorException(errorReason, context)))
                  .ErrorCase("0", CreateErrorCase("unexpected error", (errorReason, context) => new EtxResponseErrorException(errorReason, context))))
              .ExecuteAsync(cancellationToken).ConfigureAwait(false);

        /// <summary>
        /// This endpoint creates a new configuration in the system. The data for the new configuration should be provided as JSON in the body of the POST request. The system will return with a unique ID for the configuration, which is needed for any further manipulation (update or delete) of the configuration.
        /// Note: The user needs to authenticate with their ThingSpace credentials using the Access/Bearer and Session/M2M tokens in order to call this API.
        /// </summary>
        /// <param name="vendorID">Required parameter: The vendor's identifier.</param>
        /// <param name="body">Required parameter: .</param>
        /// <returns>Returns the ApiResponse of Models.GeoFenceConfigurationResponse response from the API call.</returns>
        public ApiResponse<Models.GeoFenceConfigurationResponse> CreateConfiguration(
                string vendorID,
                Models.GeoFenceConfigurationRequest body)
            => CoreHelper.RunTask(CreateConfigurationAsync(vendorID, body));

        /// <summary>
        /// This endpoint creates a new configuration in the system. The data for the new configuration should be provided as JSON in the body of the POST request. The system will return with a unique ID for the configuration, which is needed for any further manipulation (update or delete) of the configuration.
        /// Note: The user needs to authenticate with their ThingSpace credentials using the Access/Bearer and Session/M2M tokens in order to call this API.
        /// </summary>
        /// <param name="vendorID">Required parameter: The vendor's identifier.</param>
        /// <param name="body">Required parameter: .</param>
        /// <param name="cancellationToken"> cancellationToken. </param>
        /// <returns>Returns the ApiResponse of Models.GeoFenceConfigurationResponse response from the API call.</returns>
        public async Task<ApiResponse<Models.GeoFenceConfigurationResponse>> CreateConfigurationAsync(
                string vendorID,
                Models.GeoFenceConfigurationRequest body,
                CancellationToken cancellationToken = default)
            => await CreateApiCall<Models.GeoFenceConfigurationResponse>()
              .Server(Server.IMPServer)
              .RequestBuilder(requestBuilder => requestBuilder
                  .Setup(HttpMethod.Post, "/api/v1/application/configurations/geofence")
                  .WithAndAuth(andAuth => andAuth
                      .Add("thingspace_oauth")
                      .Add("sessionToken")
                  )
                  .Parameters(parameters => parameters
                      .Body(b => b.Setup(body))
                      .Header(header => header.Setup("VendorID", vendorID))
                      .Header(header => header.Setup("Content-Type", "application/json"))))
              .ResponseHandler(responseHandler => responseHandler
                  .ErrorCase("400", CreateErrorCase("Invalid configuration", (errorReason, context) => new EtxResponseErrorException(errorReason, context)))
                  .ErrorCase("403", CreateErrorCase("Forbidden", (errorReason, context) => new EtxResponseErrorException(errorReason, context)))
                  .ErrorCase("429", CreateErrorCase("Too many requests", (errorReason, context) => new EtxResponseErrorException(errorReason, context)))
                  .ErrorCase("0", CreateErrorCase("unexpected error", (errorReason, context) => new EtxResponseErrorException(errorReason, context))))
              .ExecuteAsync(cancellationToken).ConfigureAwait(false);

        /// <summary>
        /// This endpoint updates an existing configuration. Similar to POST, the updated data for the configuration should be provided as JSON in the body of the PUT request. The configuration ID parameter, which was provided by the POST (create) operation, is required to do any updates on the configuration.
        /// Note: The user needs to authenticate with their ThingSpace credentials using the Access/Bearer and Session/M2M tokens in order to call this API.
        /// </summary>
        /// <param name="vendorID">Required parameter: The vendor's identifier.</param>
        /// <param name="id">Required parameter: The configuration identifier.</param>
        /// <param name="body">Required parameter: .</param>
        public void UpdateConfiguration(
                string vendorID,
                string id,
                Models.GeoFenceConfigurationUpdateRequest body)
            => CoreHelper.RunVoidTask(UpdateConfigurationAsync(vendorID, id, body));

        /// <summary>
        /// This endpoint updates an existing configuration. Similar to POST, the updated data for the configuration should be provided as JSON in the body of the PUT request. The configuration ID parameter, which was provided by the POST (create) operation, is required to do any updates on the configuration.
        /// Note: The user needs to authenticate with their ThingSpace credentials using the Access/Bearer and Session/M2M tokens in order to call this API.
        /// </summary>
        /// <param name="vendorID">Required parameter: The vendor's identifier.</param>
        /// <param name="id">Required parameter: The configuration identifier.</param>
        /// <param name="body">Required parameter: .</param>
        /// <param name="cancellationToken"> cancellationToken. </param>
        /// <returns>Returns the void response from the API call.</returns>
        public async Task UpdateConfigurationAsync(
                string vendorID,
                string id,
                Models.GeoFenceConfigurationUpdateRequest body,
                CancellationToken cancellationToken = default)
            => await CreateApiCall<VoidType>()
              .Server(Server.IMPServer)
              .RequestBuilder(requestBuilder => requestBuilder
                  .Setup(HttpMethod.Put, "/api/v1/application/configurations/geofence")
                  .WithAndAuth(andAuth => andAuth
                      .Add("thingspace_oauth")
                      .Add("sessionToken")
                  )
                  .Parameters(parameters => parameters
                      .Body(b => b.Setup(body))
                      .Header(header => header.Setup("VendorID", vendorID))
                      .Query(query => query.Setup("id", id))
                      .Header(header => header.Setup("Content-Type", "application/json"))))
              .ResponseHandler(responseHandler => responseHandler
                  .ErrorCase("400", CreateErrorCase("Invalid configuration", (errorReason, context) => new EtxResponseErrorException(errorReason, context)))
                  .ErrorCase("403", CreateErrorCase("Forbidden", (errorReason, context) => new EtxResponseErrorException(errorReason, context)))
                  .ErrorCase("404", CreateErrorCase("Configuration not found", (errorReason, context) => new EtxResponseErrorException(errorReason, context)))
                  .ErrorCase("429", CreateErrorCase("Too many requests", (errorReason, context) => new EtxResponseErrorException(errorReason, context)))
                  .ErrorCase("0", CreateErrorCase("unexpected error", (errorReason, context) => new EtxResponseErrorException(errorReason, context))))
              .ExecuteAsync(cancellationToken).ConfigureAwait(false);

        /// <summary>
        /// This endpoint deletes a specific configuration from the system. It requires the configuration ID parameter, which was provided by the POST (create) operation.
        /// Note: The user needs to authenticate with their ThingSpace credentials using the Access/Bearer and Session/M2M tokens in order to call this API.
        /// </summary>
        /// <param name="vendorID">Required parameter: The vendor's identifier.</param>
        /// <param name="id">Required parameter: The configuration identifier.</param>
        public void DeleteConfiguration(
                string vendorID,
                string id)
            => CoreHelper.RunVoidTask(DeleteConfigurationAsync(vendorID, id));

        /// <summary>
        /// This endpoint deletes a specific configuration from the system. It requires the configuration ID parameter, which was provided by the POST (create) operation.
        /// Note: The user needs to authenticate with their ThingSpace credentials using the Access/Bearer and Session/M2M tokens in order to call this API.
        /// </summary>
        /// <param name="vendorID">Required parameter: The vendor's identifier.</param>
        /// <param name="id">Required parameter: The configuration identifier.</param>
        /// <param name="cancellationToken"> cancellationToken. </param>
        /// <returns>Returns the void response from the API call.</returns>
        public async Task DeleteConfigurationAsync(
                string vendorID,
                string id,
                CancellationToken cancellationToken = default)
            => await CreateApiCall<VoidType>()
              .Server(Server.IMPServer)
              .RequestBuilder(requestBuilder => requestBuilder
                  .Setup(HttpMethod.Delete, "/api/v1/application/configurations/geofence")
                  .WithAndAuth(andAuth => andAuth
                      .Add("thingspace_oauth")
                      .Add("sessionToken")
                  )
                  .Parameters(parameters => parameters
                      .Header(header => header.Setup("VendorID", vendorID))
                      .Query(query => query.Setup("id", id))))
              .ResponseHandler(responseHandler => responseHandler
                  .ErrorCase("403", CreateErrorCase("Forbidden", (errorReason, context) => new EtxResponseErrorException(errorReason, context)))
                  .ErrorCase("429", CreateErrorCase("Too many requests", (errorReason, context) => new EtxResponseErrorException(errorReason, context)))
                  .ErrorCase("0", CreateErrorCase("unexpected error", (errorReason, context) => new EtxResponseErrorException(errorReason, context))))
              .ExecuteAsync(cancellationToken).ConfigureAwait(false);

        /// <summary>
        /// With this API call the user (client) registers its device or software service to the ETX system. Therefore, when a connection is initiated from the device or software service to the ETX system along with the credential provided by this registration call, then the connection will be authorized.
        /// - The user can register multiple devices or software services, which can all be used at the same time.
        /// - There rules set in the system that limit the type and subtype of the clients that are allowed to be registered under the VendorID. The rules are created based ont he agreement between the Vendor and Verizon.
        /// - The user will only be able to register a limited number of devices or software services under the same VendorID. This registration limit is specified by the agreement between the Vendor and Verizon.
        /// Note: The user needs to authenticate with their ThingSpace credentials using the Access/Bearer and Session/M2M tokens in order to call this API.
        /// </summary>
        /// <param name="body">Required parameter: .</param>
        /// <param name="xTransactionId">Optional parameter: Optional transaction identifier for tracing requests. If not provided, the application will generate one..</param>
        /// <returns>Returns the ApiResponse of Models.ClientRegistrationResponse response from the API call.</returns>
        public ApiResponse<Models.ClientRegistrationResponse> RegisterETXDevice(
                Models.ClientRegistrationRequestV2 body,
                Guid? xTransactionId = null)
            => CoreHelper.RunTask(RegisterETXDeviceAsync(body, xTransactionId));

        /// <summary>
        /// With this API call the user (client) registers its device or software service to the ETX system. Therefore, when a connection is initiated from the device or software service to the ETX system along with the credential provided by this registration call, then the connection will be authorized.
        /// - The user can register multiple devices or software services, which can all be used at the same time.
        /// - There rules set in the system that limit the type and subtype of the clients that are allowed to be registered under the VendorID. The rules are created based ont he agreement between the Vendor and Verizon.
        /// - The user will only be able to register a limited number of devices or software services under the same VendorID. This registration limit is specified by the agreement between the Vendor and Verizon.
        /// Note: The user needs to authenticate with their ThingSpace credentials using the Access/Bearer and Session/M2M tokens in order to call this API.
        /// </summary>
        /// <param name="body">Required parameter: .</param>
        /// <param name="xTransactionId">Optional parameter: Optional transaction identifier for tracing requests. If not provided, the application will generate one..</param>
        /// <param name="cancellationToken"> cancellationToken. </param>
        /// <returns>Returns the ApiResponse of Models.ClientRegistrationResponse response from the API call.</returns>
        public async Task<ApiResponse<Models.ClientRegistrationResponse>> RegisterETXDeviceAsync(
                Models.ClientRegistrationRequestV2 body,
                Guid? xTransactionId = null,
                CancellationToken cancellationToken = default)
            => await CreateApiCall<Models.ClientRegistrationResponse>()
              .Server(Server.IMPServer)
              .RequestBuilder(requestBuilder => requestBuilder
                  .Setup(HttpMethod.Post, "/api/v2/clients/registration")
                  .WithAndAuth(andAuth => andAuth
                      .Add("thingspace_oauth")
                      .Add("sessionToken")
                  )
                  .Parameters(parameters => parameters
                      .Body(b => b.Setup(body))
                      .Header(header => header.Setup("Content-Type", "application/json"))
                      .Header(header => header.Setup("X-Transaction-Id", xTransactionId))))
              .ResponseHandler(responseHandler => responseHandler
                  .ErrorCase("400", CreateErrorCase("Invalid Request", (errorReason, context) => new ETXRespondingErrorException(errorReason, context)))
                  .ErrorCase("401", CreateErrorCase("Unauthorized Request", (errorReason, context) => new ETXRespondingErrorException(errorReason, context)))
                  .ErrorCase("403", CreateErrorCase("Forbidden Request", (errorReason, context) => new ETXRespondingErrorException(errorReason, context)))
                  .ErrorCase("429", CreateErrorCase("Too Many Requests", (errorReason, context) => new ETXRespondingErrorException(errorReason, context)))
                  .ErrorCase("503", CreateErrorCase("Internal Server Error", (errorReason, context) => new ETXRespondingErrorException(errorReason, context)))
                  .ErrorCase("0", CreateErrorCase("Forbidden", (errorReason, context) => new ETXRespondingErrorException(errorReason, context))))
              .ExecuteAsync(cancellationToken).ConfigureAwait(false);

        /// <summary>
        /// With this API call the user (client) can:.
        /// - renew the certificate of a device or software service in the ETX system if the original certificate has expired. If the client's certificate expired or going to expire within 30 days and new certificate will be issued. If the certificate expires more than 30 days, the current certificate will be returned to the client.
        /// - complete its device or software service registration to the ETX system if the original registration request was not successful because of a pending certificate generation. Whenever the user receives a "client registration is pending" response (HTTP 202) from POST /clients/registration call. The client should initiate this PUT API call to finish the registration process and get the required certificate.
        /// Note: The user needs to authenticate with their ThingSpace credentials using the Access/Bearer and Session/M2M tokens in order to call this API.
        /// </summary>
        /// <param name="etxDeviceID">Required parameter: .</param>
        /// <param name="etxVendorID">Required parameter: The VendorID set during the Vendor registration call..</param>
        /// <param name="xTransactionId">Optional parameter: Optional transaction identifier for tracing requests. If not provided, the application will generate one..</param>
        /// <param name="body">Optional parameter: .</param>
        /// <returns>Returns the ApiResponse of Models.ClientRegistrationResponse response from the API call.</returns>
        public ApiResponse<Models.ClientRegistrationResponse> RenewETXDevice(
                Guid etxDeviceID,
                string etxVendorID,
                Guid? xTransactionId = null,
                object body = null)
            => CoreHelper.RunTask(RenewETXDeviceAsync(etxDeviceID, etxVendorID, xTransactionId, body));

        /// <summary>
        /// With this API call the user (client) can:.
        /// - renew the certificate of a device or software service in the ETX system if the original certificate has expired. If the client's certificate expired or going to expire within 30 days and new certificate will be issued. If the certificate expires more than 30 days, the current certificate will be returned to the client.
        /// - complete its device or software service registration to the ETX system if the original registration request was not successful because of a pending certificate generation. Whenever the user receives a "client registration is pending" response (HTTP 202) from POST /clients/registration call. The client should initiate this PUT API call to finish the registration process and get the required certificate.
        /// Note: The user needs to authenticate with their ThingSpace credentials using the Access/Bearer and Session/M2M tokens in order to call this API.
        /// </summary>
        /// <param name="etxDeviceID">Required parameter: .</param>
        /// <param name="etxVendorID">Required parameter: The VendorID set during the Vendor registration call..</param>
        /// <param name="xTransactionId">Optional parameter: Optional transaction identifier for tracing requests. If not provided, the application will generate one..</param>
        /// <param name="body">Optional parameter: .</param>
        /// <param name="cancellationToken"> cancellationToken. </param>
        /// <returns>Returns the ApiResponse of Models.ClientRegistrationResponse response from the API call.</returns>
        public async Task<ApiResponse<Models.ClientRegistrationResponse>> RenewETXDeviceAsync(
                Guid etxDeviceID,
                string etxVendorID,
                Guid? xTransactionId = null,
                object body = null,
                CancellationToken cancellationToken = default)
            => await CreateApiCall<Models.ClientRegistrationResponse>()
              .Server(Server.IMPServer)
              .RequestBuilder(requestBuilder => requestBuilder
                  .Setup(HttpMethod.Put, "/api/v2/clients/registration")
                  .WithAndAuth(andAuth => andAuth
                      .Add("thingspace_oauth")
                      .Add("sessionToken")
                  )
                  .Parameters(parameters => parameters
                      .Body(b => b.Setup(body))
                      .Header(header => header.Setup("etxDeviceID", etxDeviceID))
                      .Header(header => header.Setup("etxVendorID", etxVendorID))
                      .Header(header => header.Setup("Content-Type", "application/json"))
                      .Header(header => header.Setup("X-Transaction-Id", xTransactionId))))
              .ResponseHandler(responseHandler => responseHandler
                  .ErrorCase("400", CreateErrorCase("Invalid Request", (errorReason, context) => new ETXRespondingErrorException(errorReason, context)))
                  .ErrorCase("401", CreateErrorCase("Unauthorized Request", (errorReason, context) => new ETXRespondingErrorException(errorReason, context)))
                  .ErrorCase("403", CreateErrorCase("Forbidden Request", (errorReason, context) => new ETXRespondingErrorException(errorReason, context)))
                  .ErrorCase("429", CreateErrorCase("Too Many Requests", (errorReason, context) => new ETXRespondingErrorException(errorReason, context)))
                  .ErrorCase("503", CreateErrorCase("Internal Server Error", (errorReason, context) => new ETXRespondingErrorException(errorReason, context)))
                  .ErrorCase("0", CreateErrorCase("Forbidden", (errorReason, context) => new ETXRespondingErrorException(errorReason, context))))
              .ExecuteAsync(cancellationToken).ConfigureAwait(false);

        /// <summary>
        /// With this API call the user (client) can unregister its devices and software services from the ETX system. The unregistered devices and services will no longer be able to use the ETX Message Exchange.
        /// Note: The user needs to authenticate with their ThingSpace credentials using the Access/Bearer and Session/M2M tokens in order to call this API.
        /// </summary>
        /// <param name="etxVendorID">Required parameter: The VendorID set during the Vendor registration call..</param>
        /// <param name="deviceIDs">Required parameter: The list of device IDs and software service IDs to be unregistered.</param>
        /// <param name="xTransactionId">Optional parameter: Optional transaction identifier for tracing requests. If not provided, the application will generate one..</param>
        public void UnregisterETXDevice(
                string etxVendorID,
                List<Guid> deviceIDs,
                Guid? xTransactionId = null)
            => CoreHelper.RunVoidTask(UnregisterETXDeviceAsync(etxVendorID, deviceIDs, xTransactionId));

        /// <summary>
        /// With this API call the user (client) can unregister its devices and software services from the ETX system. The unregistered devices and services will no longer be able to use the ETX Message Exchange.
        /// Note: The user needs to authenticate with their ThingSpace credentials using the Access/Bearer and Session/M2M tokens in order to call this API.
        /// </summary>
        /// <param name="etxVendorID">Required parameter: The VendorID set during the Vendor registration call..</param>
        /// <param name="deviceIDs">Required parameter: The list of device IDs and software service IDs to be unregistered.</param>
        /// <param name="xTransactionId">Optional parameter: Optional transaction identifier for tracing requests. If not provided, the application will generate one..</param>
        /// <param name="cancellationToken"> cancellationToken. </param>
        /// <returns>Returns the void response from the API call.</returns>
        public async Task UnregisterETXDeviceAsync(
                string etxVendorID,
                List<Guid> deviceIDs,
                Guid? xTransactionId = null,
                CancellationToken cancellationToken = default)
            => await CreateApiCall<VoidType>()
              .Server(Server.IMPServer)
              .RequestBuilder(requestBuilder => requestBuilder
                  .Setup(HttpMethod.Delete, "/api/v2/clients/registration")
                  .WithAndAuth(andAuth => andAuth
                      .Add("thingspace_oauth")
                      .Add("sessionToken")
                  )
                  .Parameters(parameters => parameters
                      .Header(header => header.Setup("etxVendorID", etxVendorID))
                      .Query(query => query.Setup("DeviceIDs", deviceIDs))
                      .Header(header => header.Setup("X-Transaction-Id", xTransactionId))))
              .ResponseHandler(responseHandler => responseHandler
                  .ErrorCase("400", CreateErrorCase("Invalid Request", (errorReason, context) => new ETXRespondingErrorException(errorReason, context)))
                  .ErrorCase("401", CreateErrorCase("Unauthorized Request", (errorReason, context) => new ETXRespondingErrorException(errorReason, context)))
                  .ErrorCase("403", CreateErrorCase("Forbidden Request", (errorReason, context) => new ETXRespondingErrorException(errorReason, context)))
                  .ErrorCase("429", CreateErrorCase("Too Many Requests", (errorReason, context) => new ETXRespondingErrorException(errorReason, context)))
                  .ErrorCase("503", CreateErrorCase("Internal Server Error", (errorReason, context) => new ETXRespondingErrorException(errorReason, context)))
                  .ErrorCase("0", CreateErrorCase("Forbidden", (errorReason, context) => new ETXRespondingErrorException(errorReason, context))))
              .ExecuteAsync(cancellationToken).ConfigureAwait(false);

        /// <summary>
        /// With this API call the user can check the certificate of the device. At least one of the DeviceID, IMEI, ICCID or IMSI is required to make the call.
        /// Note: The user needs to authenticate with their ThingSpace credentials using the Access/Bearer and Session/M2M tokens in order to call this API.
        /// </summary>
        /// <param name="etxID">Required parameter: One of the following IDs is required- DeviceID, IMEI, ICCID, IMSI. If more than one ID is provided, the API will return the certificate for the first ID found. The IDs are evaluated in the following order: DeviceID, IMEI, ICCID, IMSI. If the first provided ID is not found, the API will return an error..</param>
        /// <param name="xTransactionId">Optional parameter: Optional transaction identifier for tracing requests. If not provided, the application will generate one..</param>
        /// <returns>Returns the ApiResponse of Models.ClientPersistenceResponse response from the API call.</returns>
        public ApiResponse<Models.ClientPersistenceResponse> GetETXDeviceCertificate(
                Models.EtxID etxID,
                Guid? xTransactionId = null)
            => CoreHelper.RunTask(GetETXDeviceCertificateAsync(etxID, xTransactionId));

        /// <summary>
        /// With this API call the user can check the certificate of the device. At least one of the DeviceID, IMEI, ICCID or IMSI is required to make the call.
        /// Note: The user needs to authenticate with their ThingSpace credentials using the Access/Bearer and Session/M2M tokens in order to call this API.
        /// </summary>
        /// <param name="etxID">Required parameter: One of the following IDs is required- DeviceID, IMEI, ICCID, IMSI. If more than one ID is provided, the API will return the certificate for the first ID found. The IDs are evaluated in the following order: DeviceID, IMEI, ICCID, IMSI. If the first provided ID is not found, the API will return an error..</param>
        /// <param name="xTransactionId">Optional parameter: Optional transaction identifier for tracing requests. If not provided, the application will generate one..</param>
        /// <param name="cancellationToken"> cancellationToken. </param>
        /// <returns>Returns the ApiResponse of Models.ClientPersistenceResponse response from the API call.</returns>
        public async Task<ApiResponse<Models.ClientPersistenceResponse>> GetETXDeviceCertificateAsync(
                Models.EtxID etxID,
                Guid? xTransactionId = null,
                CancellationToken cancellationToken = default)
            => await CreateApiCall<Models.ClientPersistenceResponse>()
              .Server(Server.IMPServer)
              .RequestBuilder(requestBuilder => requestBuilder
                  .Setup(HttpMethod.Get, "/api/v2/clients/registration")
                  .WithAndAuth(andAuth => andAuth
                      .Add("thingspace_oauth")
                      .Add("sessionToken")
                  )
                  .Parameters(parameters => parameters
                      .Query(query => query.Setup("etxID", etxID))
                      .Header(header => header.Setup("X-Transaction-Id", xTransactionId))))
              .ResponseHandler(responseHandler => responseHandler
                  .ErrorCase("400", CreateErrorCase("Invalid request", (errorReason, context) => new ETXRespondingErrorException(errorReason, context)))
                  .ErrorCase("401", CreateErrorCase("Unauthorized", (errorReason, context) => new ETXRespondingErrorException(errorReason, context)))
                  .ErrorCase("403", CreateErrorCase("Forbidden Request", (errorReason, context) => new ETXRespondingErrorException(errorReason, context)))
                  .ErrorCase("404", CreateErrorCase("Not Found", (errorReason, context) => new ETXRespondingErrorException(errorReason, context)))
                  .ErrorCase("429", CreateErrorCase("Too Many Requests", (errorReason, context) => new ETXRespondingErrorException(errorReason, context)))
                  .ErrorCase("500", CreateErrorCase("Internal server Error", (errorReason, context) => new ETXRespondingErrorException(errorReason, context)))
                  .ErrorCase("0", CreateErrorCase("Forbidden", (errorReason, context) => new ETXRespondingErrorException(errorReason, context))))
              .ExecuteAsync(cancellationToken).ConfigureAwait(false);

        /// <summary>
        /// With this API call the device or software service requests the MQTT URL for the location that it needs to connect. To determine the proper URL the device or software service needs to provide its ID (the one that was provided in the registration request), location (GPS coordinates), and whether it is on the Verizon cellular network or not.
        /// Note: The user needs to authenticate with their ThingSpace credentials using the Access/Bearer and Session/M2M tokens in order to call this API.
        /// </summary>
        /// <param name="etxVendorID">Required parameter: The VendorID set during the Vendor registration call..</param>
        /// <param name="body">Required parameter: .</param>
        /// <param name="xTransactionId">Optional parameter: Optional transaction identifier for tracing requests. If not provided, the application will generate one..</param>
        /// <returns>Returns the ApiResponse of Models.ConnectionResponse response from the API call.</returns>
        public ApiResponse<Models.ConnectionResponse> RetrieveMQTTURL(
                string etxVendorID,
                Models.ConnectionRequest body,
                Guid? xTransactionId = null)
            => CoreHelper.RunTask(RetrieveMQTTURLAsync(etxVendorID, body, xTransactionId));

        /// <summary>
        /// With this API call the device or software service requests the MQTT URL for the location that it needs to connect. To determine the proper URL the device or software service needs to provide its ID (the one that was provided in the registration request), location (GPS coordinates), and whether it is on the Verizon cellular network or not.
        /// Note: The user needs to authenticate with their ThingSpace credentials using the Access/Bearer and Session/M2M tokens in order to call this API.
        /// </summary>
        /// <param name="etxVendorID">Required parameter: The VendorID set during the Vendor registration call..</param>
        /// <param name="body">Required parameter: .</param>
        /// <param name="xTransactionId">Optional parameter: Optional transaction identifier for tracing requests. If not provided, the application will generate one..</param>
        /// <param name="cancellationToken"> cancellationToken. </param>
        /// <returns>Returns the ApiResponse of Models.ConnectionResponse response from the API call.</returns>
        public async Task<ApiResponse<Models.ConnectionResponse>> RetrieveMQTTURLAsync(
                string etxVendorID,
                Models.ConnectionRequest body,
                Guid? xTransactionId = null,
                CancellationToken cancellationToken = default)
            => await CreateApiCall<Models.ConnectionResponse>()
              .Server(Server.IMPServer)
              .RequestBuilder(requestBuilder => requestBuilder
                  .Setup(HttpMethod.Post, "/api/v2/clients/connection")
                  .WithAndAuth(andAuth => andAuth
                      .Add("thingspace_oauth")
                      .Add("sessionToken")
                  )
                  .Parameters(parameters => parameters
                      .Body(b => b.Setup(body))
                      .Header(header => header.Setup("etxVendorID", etxVendorID))
                      .Header(header => header.Setup("Content-Type", "application/json"))
                      .Header(header => header.Setup("X-Transaction-Id", xTransactionId))))
              .ResponseHandler(responseHandler => responseHandler
                  .ErrorCase("400", CreateErrorCase("Invalid request", (errorReason, context) => new ETXRespondingErrorException(errorReason, context)))
                  .ErrorCase("401", CreateErrorCase("Unauthorized", (errorReason, context) => new ETXRespondingErrorException(errorReason, context)))
                  .ErrorCase("403", CreateErrorCase("Forbidden Request", (errorReason, context) => new ETXRespondingErrorException(errorReason, context)))
                  .ErrorCase("429", CreateErrorCase("Too Many Requests", (errorReason, context) => new ETXRespondingErrorException(errorReason, context)))
                  .ErrorCase("503", CreateErrorCase("Internal server Error", (errorReason, context) => new ETXRespondingErrorException(errorReason, context)))
                  .ErrorCase("0", CreateErrorCase("Forbidden", (errorReason, context) => new ETXRespondingErrorException(errorReason, context))))
              .ExecuteAsync(cancellationToken).ConfigureAwait(false);

        /// <summary>
        /// With this API call the device or software service requests the MQTT URL for the location that it needs to connect. To determine the proper URL the device or software service needs to provide its ID (the one that was provided in the registration request), location (GPS coordinates), and whether it is on the Verizon cellular network or not.
        /// If there are multiple MECs that serve the location of the client all options are provided in the response, and the client is free to choose which MEC they want to connect.
        /// Note: The user needs to authenticate with their ThingSpace credentials using the Access/Bearer and Session/M2M tokens in order to call this API.
        /// </summary>
        /// <param name="etxVendorID">Required parameter: The VendorID set during the Vendor registration call..</param>
        /// <param name="body">Required parameter: .</param>
        /// <param name="xTransactionId">Optional parameter: Optional transaction identifier for tracing requests. If not provided, the application will generate one..</param>
        /// <returns>Returns the ApiResponse of Models.ConnectionResponseV3 response from the API call.</returns>
        public ApiResponse<Models.ConnectionResponseV3> RetrieveMQTTURLMultiMEC(
                string etxVendorID,
                Models.ConnectionRequest body,
                Guid? xTransactionId = null)
            => CoreHelper.RunTask(RetrieveMQTTURLMultiMECAsync(etxVendorID, body, xTransactionId));

        /// <summary>
        /// With this API call the device or software service requests the MQTT URL for the location that it needs to connect. To determine the proper URL the device or software service needs to provide its ID (the one that was provided in the registration request), location (GPS coordinates), and whether it is on the Verizon cellular network or not.
        /// If there are multiple MECs that serve the location of the client all options are provided in the response, and the client is free to choose which MEC they want to connect.
        /// Note: The user needs to authenticate with their ThingSpace credentials using the Access/Bearer and Session/M2M tokens in order to call this API.
        /// </summary>
        /// <param name="etxVendorID">Required parameter: The VendorID set during the Vendor registration call..</param>
        /// <param name="body">Required parameter: .</param>
        /// <param name="xTransactionId">Optional parameter: Optional transaction identifier for tracing requests. If not provided, the application will generate one..</param>
        /// <param name="cancellationToken"> cancellationToken. </param>
        /// <returns>Returns the ApiResponse of Models.ConnectionResponseV3 response from the API call.</returns>
        public async Task<ApiResponse<Models.ConnectionResponseV3>> RetrieveMQTTURLMultiMECAsync(
                string etxVendorID,
                Models.ConnectionRequest body,
                Guid? xTransactionId = null,
                CancellationToken cancellationToken = default)
            => await CreateApiCall<Models.ConnectionResponseV3>()
              .Server(Server.IMPServer)
              .RequestBuilder(requestBuilder => requestBuilder
                  .Setup(HttpMethod.Post, "/api/v3/clients/connection")
                  .WithAndAuth(andAuth => andAuth
                      .Add("thingspace_oauth")
                      .Add("sessionToken")
                  )
                  .Parameters(parameters => parameters
                      .Body(b => b.Setup(body))
                      .Header(header => header.Setup("etxVendorID", etxVendorID))
                      .Header(header => header.Setup("Content-Type", "application/json"))
                      .Header(header => header.Setup("X-Transaction-Id", xTransactionId))))
              .ResponseHandler(responseHandler => responseHandler
                  .ErrorCase("400", CreateErrorCase("Invalid request", (errorReason, context) => new ETXRespondingErrorException(errorReason, context)))
                  .ErrorCase("401", CreateErrorCase("Unauthorized", (errorReason, context) => new ETXRespondingErrorException(errorReason, context)))
                  .ErrorCase("403", CreateErrorCase("Forbidden Request", (errorReason, context) => new ETXRespondingErrorException(errorReason, context)))
                  .ErrorCase("429", CreateErrorCase("Too Many Requests", (errorReason, context) => new ETXRespondingErrorException(errorReason, context)))
                  .ErrorCase("503", CreateErrorCase("Internal server Error", (errorReason, context) => new ETXRespondingErrorException(errorReason, context)))
                  .ErrorCase("0", CreateErrorCase("Forbidden", (errorReason, context) => new ETXRespondingErrorException(errorReason, context))))
              .ExecuteAsync(cancellationToken).ConfigureAwait(false);
    }
}